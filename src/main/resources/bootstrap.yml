spring:
  profiles:
      active: ${CONFIG_PROFILE:dev}
  application:
    name: app-one
  cloud:
    config:
      uri: http://localhost:9706
      username: user
      password: ${CONFIG_LOGIN_PWD:12345}
      fail-fast: true
server:
  port: 9704