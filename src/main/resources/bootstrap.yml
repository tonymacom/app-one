spring:
  profiles:
      active: ${CONFIG_PROFILE:dev}
  application:
    name: app-one
  cloud:
    config:
      uri: http://config-server:9706
      username: user
      password: ${CONFIG_LOGIN_PWD:12345}
      fail-fast: true #连接不上早知道
server:
  port: 9704
logging:
  file: /home/log/app-one.log